# 开发任务体系

## 总规则

- 一个模块一个任务。任务编号只对应一级业务模块或基础模块，不再对应单个页面。
- 一个页面一个采集记录。页面是模块任务下的子项，不单独升级为大任务。
- 每次开发只做一个清晰任务包，不把未采集页面当成已完成页面。
- 每个任务包结束必须验证、提交、推送到 GitHub。

## 任务状态

- 待采集：只有菜单和参考路径，还没有页面细节。
- 已采集：已记录筛选、表格、按钮、弹窗、状态和流程。
- 开发中：正在做数据库、接口、管理端页面或小程序。
- 已验证：构建或后端打包通过。
- 已备份：已提交并推送到 GitHub。

## 当前任务包

### T01 客户模块

- 状态：已备份。
- 参考模块：客户。
- 目标：
  - 按参考系统完成客户模块规格、页面、接口和数据结构。
  - 已完成子项：客户 / 客户管理 / 客户信息。
  - 后续子项：潜在商机、客户跟进、会员等级维护、会员信息管理、会员参数、客户报表、跟进类别。
- 验证：
  - 后端 `mvn -q -DskipTests package` 已通过。
  - 前端 `npm run build` 已通过。

### T02 采购模块

- 状态：已备份。
- 参考模块：采购。
- 目标：
  - 按参考系统完成采购模块规格、页面、接口和数据结构。
  - 覆盖采购业务和采购报表：请购单、采购订单、采购单、采购退货单、采购发票登记、采购汇总表、采购订单明细、采购单明细、采购开票统计。
- 已完成子项：
  - 采购 / 采购业务 / 采购订单。
- 采购订单范围：
  - 已采集列表筛选：采购订单号、供应商、审核状态、商品、高级搜索。
  - 已采集工具栏：新增、复制、审核、反审核、生成采购单、批量打印、设置、导出。
  - 已采集表格列：订单号、订单日期、供应商、交货日期、结款方式、数量、金额、税额、其他费用、合计金额、供应商单号、归属项目、附件、备注、状态、请购人、请购单号、制单人、制单时间、审核人、审核时间、采购单、转采购数量。
  - 已接入管理端参考页面 `/purchase/orders`。
- 后续子项：
  - 请购单。
  - 采购单。
  - 采购退货单。
  - 采购发票登记。
  - 采购报表。
- 验证：
  - 前端 `npm run build` 已通过。

### T03 销售模块

- 状态：已验证。
- 参考模块：销售。
- 目标：
  - 按参考系统完成销售模块规格、页面、接口和数据结构。
  - 覆盖批发业务、零售业务、销售明细和销售报表。
- 已完成子项：
  - 销售 / 批发业务 / 销售订单。
- 销售订单范围：
  - 已采集页签：正常、已关闭。
  - 已采集列表筛选：订单日期、客户、商品、审核状态、高级搜索。
  - 已采集工具栏：新增、复制、审核、反审核、生成单据、生成生产任务、批量打印、更多操作、设置、导出。
  - 已采集表格列：销售订单号、订单日期、客户名称、客户订单号、数量、金额、订金、附件、交货日期、销售员、状态、审核人、审核时间、已收款、未收款、销售单、出库状态、采购单、转销售数量、生产任务、委外加工、打印次数、备注、制单人、制单日期。
  - 已接入管理端参考页面 `/sales/orders`。
- 后续子项：
  - 报价单。
  - 网店订单。
  - 销售单。
  - 销售退货单。
  - 销售返修单。
  - 销售发票登记。
  - 销售回款情况。
  - 零售收银和零售收银历史。
  - 销售明细和销售报表。
- 验证：
  - 前端 `npm run build` 已通过。

### T04 生产模块

- 状态：待采集。
- 参考模块：生产。
- 目标：按参考系统完成生产模块规格、页面、接口和数据结构。
- 子项：
  - 自制业务：生产任务、生产领料、用料登记、工序汇报、生产质检、成品入库。
  - 委外业务：委外任务、物料出库、提请付款、成品质检、成品入库、委外付款情况。
  - 其他：工序管理、BOM 管理、BOM 模板、不良品项、计时工资、计件工资、工序汇报记录。
  - 生产报表：生产看板、生产成本统计、订单进度跟踪表、委外进度跟踪表、生产进度跟踪表、物料追踪表、缺料追踪表、计件工资统计表、计时工资统计表。

### T05 库管模块

- 状态：待采集。
- 参考模块：库管。
- 目标：按参考系统完成库管模块规格、页面、接口和数据结构。
- 子项：
  - 库管单据：入库管理、出库管理、盘点单、调拨单、组装拆卸单、报损单、补货申请、补货审理。
  - 库存查询：商品库存查询、序列号跟踪、批号/保质期查询、在途商品查询。
  - 仓库报表：仓库库存统计、商品出入库统计、项目出入库统计、出入库流水、入库单明细、出库单明细、调拨单明细。

## 后续任务包

- T06 基础模块：商品信息、商品属性、供应商管理、项目信息、物流公司、条形码打印、序列号打印、仓库资料、员工部门、结算账户等业务基础。
- T07 物流模块：物流发货、物流公司、物流查询、物流平台设置。
- T08 财务模块：往来账务、资金业务、费用报销、财务报表。
- T09 总账模块：凭证、账簿、结账、报表、科目和总账参数。
- T10 人事模块：薪资设置、业绩目标、月度工资、工作奖惩、考勤、打卡、计件工资和员工工资。
- T11 设置模块：期初设置和标记设置。

## 每轮交付格式

每轮结束输出：

- 完成了哪些任务。
- 改了哪些文件。
- 跑了哪些验证。
- GitHub 提交号。
- 下一步任务编号。
